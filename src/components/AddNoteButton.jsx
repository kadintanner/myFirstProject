const AddNoteButton = () => {

    return (
        <tr>
            <td colSpan={4} >
                <button>Add New Note</button>
            </td>
        </tr>
    )
}

export default AddNoteButton
